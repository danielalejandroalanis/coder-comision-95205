{
  "name": "back-reservas-coder",
  "version": "1.0.0",
  "type": "module",
  "description": "Proyecto de reservas y turnos",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
